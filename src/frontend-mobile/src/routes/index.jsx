import {AppRoutes} from "./app.routes";
import HeaderSidebar from './app.sidebar';

export function Routes() {
  return (
    <NavigationContainer>
			<AppRoutes />
      </NavigationContainer>
  );
}
